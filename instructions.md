Edit the Section 0 variables — paths, thread count, sample names, and conditions.
The sample dictionary supports both paired-end and single-end reads (set READ2 to "SE" for SE samples).
Make it executable: chmod +x rnaseq_pipeline.sh
Run: bash rnaseq_pipeline.sh or submit via your HPC scheduler (SLURM/PBS).